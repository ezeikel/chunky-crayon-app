import { View } from "react-native";
import { Text } from "react-native-svg";

const ImageCanvas = () => {
  return (
    <View>
      <Text>Image Canvas</Text>
    </View>
  );
};

export default ImageCanvas;
